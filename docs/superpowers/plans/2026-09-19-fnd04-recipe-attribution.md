# FND-04 recipe attribution implementation plan

> For agentic workers: REQUIRED SUB-SKILL superpowers:subagent-driven-development. Owner requires a fresh builder and separate critic, review before commit, and continuous execution.

**Goal:** Replace fabricated ENTROPY1 with recipe-derived base aspects under the inspected BETA26 rules.
**Architecture:** Per-query current recipe view and private cumulative traversal/memo; reuse shared computed lookup for ingredients; support all four recipe categories; never register derived values as explicit seeds.
**Tech Stack:** Minecraft1.20.1, Forge47.3.0, Java17, existing JUnit5 and Forge GameTests.
**Spec:** docs/superpowers/specs/2026-09-19-fnd04-recipe-attribution-design.md.

## Global constraints

- Repository D:/dev/thaumcraft-shobie-review; branch codex/fnd-04-aspect-lookup; base dc17c32. Preserve reviewed container, public route, culling and tag reload repairs.
- FND04 remains ACTIVE, FND01 VERIFYING; no dependency release or full parity claim. No EULA, ordinary server, distribution, remote Git changes, dependencies or original code/assets retention.
- Read REF0015 note as evidence, subject to critic. Do not copy decompiled code. Preserve confirmed arithmetic and named modern adaptations.
- No production edit before behavioral RED. Builder/critic never commit or spawn agents. Root owns coordination docs and final commit.
- No broad seed/NBT/bonus/entity/complex-registration changes. Existing raw getter/exists remain explicit-registration only.

## Review focus

- Registry data differs by server/client/session/reload: query current providers, never retain stale source or cache empty before data exists; test recipe replacement/restoration.
- Mutable lists/stacks and repeated ingredient recursion: copy ownership and private bounded history/memo; test duplicates, asymmetric cycles, count/NBT identity and output mutation.
- Original category priority and integer/float edges: independent numerical expectations, empty earlier category suppresses fallback, first options, positive minimum and first tie; no generic sum substitute.
- Recipe adapter type erasure and remainders: actual synthetic inventory supports vanilla and arcane contract; test actual returned containers, skip unsupported types/dynamic outputs safely.
- Concurrent async GameTests: extend one existing owned reload sequence instead of launching a second pack-selection mutation; restore original selection/files and retain all tag checks.

## Task 1: Implement recipe generation and current recipe source integration

**Files:**
- Modify src/main/java/thaumcraft/common/lib/crafting/ThaumcraftCraftingManager.java: replace generator and carry private query through computed recursion.
- Create small helpers under the same package: RecipeAspectResolver.java and optionally RecipeAspectMath.java/RecipeAspectSource.java, with one clear responsibility per helper. Names may be adjusted with report explanation; no framework.
- Create src/main/java/thaumcraft/client/ClientAspectRecipeSource.java (client-only provider); modify only necessary client setup hook in src/main/java/thaumcraft/Thaumcraft.java.
- Add src/test/java/thaumcraft/common/lib/crafting/RecipeAspectMathTest.java and focused resolver/provider tests where standalone runtime permits. Prefer actual GameTests over global-registry replacement or reflection-based test seams.
- Add src/gametest/java/thaumcraft/gametest/RecipeAspectGameTests.java, fixture additions in existing AspectLookupGameTests.TestItems, and JSON recipes under src/gametest/resources/data/thaumcraft/recipes/fnd04_recipe_*.json plus optional test tags.
- Modify AspectLookupGameTests public raw-existence placeholder assertion only; modify AspectTagGameTests owned reload fixture to add recipe replacement/restoration assertions and generated pack JSON. No second racing reload.
- Create docs/agent-pipeline/fnd-04-recipe-attribution-evidence.md; generated filesystem-map.md via updater. Reference note/catalog already prepared by separate evidence builder; report corrections to root instead of unrelated edits.

**Interfaces:** Keep public generateTags(ItemStack), getObjectTags(ItemStack), and public/raw helper/API signatures. A top-level call captures one current recipe source, history, memo; recursive getObjectTags uses that same query. Resolver works with known recipe types and registry access; client classes never appear in common imports/initialization. Normal public lookups return non-null empty for absent/no-recipe. Derived memo values are copies, count-normalized by full value identity. Preserve the cumulative100-entry history contract; memo completed values before rechecking history, do not memo direct cycle/cutoff. No persistent derived cache and no mutation of explicit registration storage.

- [ ] Inspect actual classes, current JSON formats, original contract, caller paths and pinned Forge APIs. Existing source-set fixtures share one item RegisterEvent listener; use that site. Declare proposed helper/file inventory in report, notify root for material scope conflicts.
- [ ] Before production changes add real JSON crafting fixture seeded EARTH4 -> unseeded output count1; prove RecipeManager holds exact recipe/input/output, then assert public derived EARTH3 only. Add a no-recipe unseeded fixture with exact empty expectation. Run runGameTestServer, preserve build/gametest/fnd04-recipes-red-runGameTestServer.log; both must fail because old generator supplies ENTROPY1. Do not call resolver directly as sole RED proof. Send concise confirmed RED, then proceed.
- [ ] Add independent deterministic math cases before arithmetic implementation. REF0015 examples: EARTH4/count1->3; EARTH1/count1->empty; EARTH2/count2->empty; EARTH5/count4->1; METAL8 minus remainder3/count2->1; METAL2 minus remainder3/count1->1 (refused overdraw); crucible MAGIC3+required9/count3->4; requiredFIRE8/count3->0; infusion MAGIC8+required16/count2->5; arcane Vis30/count2 addsMAGIC2, Vis3/count1 addsMAGIC1 via integer division. Cover output cap500 and mutations/copy isolation. Invalid output count cannot divide by zero or overflow into fabricated positive values.
- [ ] Implement query-time recipe sourcing: current server RecipeManager/registryAccess when present, otherwise current remote-client connection through client-only provider installed in existing client setup. No active source means no persistent result. Combine only supported real legacy API-catalog recipes with loaded recipes; loaded ID wins duplicate; sort by full ID once per query. Fake/blueprint/unsupported recipe types ignored. Cache any recipe indexing only within that query.
- [ ] Implement priority/formulas exactly from spec. Use first Ingredient.getItems entry without sorting/minimizing alternatives. A private3x3 inventory supports CraftingContainer and IArcaneWorkbench and receives copied first alternatives in slot order; invoke actual remaining-items APIs for crafting/arcane. Sum computed ingredient aspects, subtract computed remainders with refusal on overdraw, normalize75%/count. Infusion center+components has no remainders. Add requiredEssentia square-root terms or arcaneVis MAGIC with integer Vis/2. Keep first positive minimum; earlier valid empty crucible/infusion suppresses crafting fallback.
- [ ] Integrate into shared computed lookup preserving explicit seeds, opted-in contained aspects, bonus additions, seven-type culling and500cap. Count-normalized copied identity retains NBT but ignores durability for damageable recursion keys. Query memo/history shared across every candidate/ingredient; no new top-level session from recursive calls. Direct recursion/depth cutoff contributes no fabricated aspects and does not cache itself. Keep query state local for simultaneous requests.
- [ ] Update existing raw-existence test's ENTROPY1 placeholder expectation to exact empty only; preserve all raw/exists/non-registration assertions. All other existing28JUnit/21GameTest behaviors remain. ComplexItem tests may not be weakened to accommodate the new generator.
- [ ] Add actual GameTests for all required category and integration cases. Use unseeded test outputs with explicit seed inputs and asymmetric expected values so category mistakes cannot pass. Minimum coverage: shaped and shapeless ordinary outputs; count division; first alternative; remainder returned by actual recipe; arcaneVis; crucible catalyst vs output-count rule; infusion center/components/Essentia; category priority including empty result; strictly smallest positive crafting result and stable tie winner; recursion chain/repeated sibling input/asymmetric cycle/depth bound; no-recipe empty; registered output wins; contained precedence unchanged; count invariance, caller mutation isolation; direct seed change visible next query; supported API-catalog recipe compatibility without polluting global state after test. Each test proves recipe setup before result assertions where it could fail vacuously.
- [ ] Extend existing real reload round trip with one unseeded recipe output initially derived EARTH3 from a seedEARTH4, then replacement recipe derives WATER6 from seedWATER8, then original EARTH3 restored. Write replacement JSON into its already-owned UUID pack; read/assert live RecipeManager ingredient/output at all stages before aspect expectations. Preserve existing tag A/C -> B/C -> A/C and iron checks. No direct production refresh helper called. Avoid global fixture/test interference.
- [ ] Include current real vanilla recipe control only after verifying seed/recipe inputs; document expected value from formula and current inputs, not as unmeasured liveBETA26 parity. Exercise repeated representative lookup work and report measured run cost or traversal bounds without flaky time assertions. Inspect query code to ensure no unbounded registry recursion or source sorting at every recursive edge.
- [ ] GREEN required: focused tests during changes, then compileJava test build plus runGameTestServer. Retain distinct logs. Verify production JAR excludes test classes/IDs/recipes/packs. Run map updater/check, pipeline validator, git diff --check; leave eula=false. No original client or ordinary dedicated server launch.
- [ ] Write concise tracked evidence with RED/GREEN counts, real reload stages, arithmetic/reference links, adaptations, client-runtime/performance limits, changed-file scope, exact logs and remaining gaps. Write full .superpowers/sdd/2026-09-19-fnd04-recipe-attribution/task-1-report.md; send root DONE/concerns. No commit. Separate critic independently checks source, contract and actual final tests before approval.

## Preflight/rulings

All4categories and modern recipe sourcing belong to this one judgeable derivation task; test-only helpers are subordinate. The unchanged caller has no Level argument, and server/client source selection is necessary rather than an optional follow-up. Current raw registry intentionally excludes generated values, so do not silently reintroduce original writes there. Per-query memo may repeat work; record this cost and any remaining performance question. Recipe-order adaptation and original persistent-cache differences are explicitly in the spec. Keep cumulative history and first ingredient choice, rather than using a more convenient backtracking/minimum-alternative algorithm.

Owner continuous execution authorizes this design/plan and fresh builder/critic loop without repeated permission. Approval-before-commit governs over skill suggestions to commit during implementation.
